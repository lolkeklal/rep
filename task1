#include <bits/stdc++.h>
 using namespace std;
 
int main()
{
	int t;
	for(cin >> t; t--;)
	{
		long long a, b, n, s;
		cin >> a >> b >> n >> s;
		if(s % n <= b and a * n + b >= s)
		{
			cout<<"YES" << endl;
		}
		else
			cout<<"NO" << endl;
	}
}
